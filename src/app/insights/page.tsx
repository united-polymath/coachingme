import { InsightsList } from './InsightsList';

export const metadata = {
  title: '인사이트 — COACHINGME Korea',
  description: 'COACHINGME의 다양한 인사이트 — 분야를 가로지르는 시야의 나눔.',
};

export default function InsightsPage() {
  return <InsightsList />;
}
